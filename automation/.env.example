# ThingsBoard instance to configure credentials on
# Alternate instance (swap in by commenting the other): TB_BASE_URL=http://203.88.135.133:8080
TB_BASE_URL=http://199.199.50.111:8080
TB_USERNAME=nlcqatest@thingsboard.org
TB_PASSWORD=cimcon

# Source of device records (clientId/userName/password per device)
DEVICES_JSON_PATH=../backend/devices.json

# Where per-device failure screenshots are written
SCREENSHOTS_DIR=./screenshots

# Set to false to watch the browser while it runs
HEADLESS=true
