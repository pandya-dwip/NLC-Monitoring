{
  "name": "nlc-thingsboard-credentials-automation",
  "version": "1.0.0",
  "description": "One-off Playwright utility that configures MQTT Basic credentials for every device in devices.json",
  "private": true,
  "scripts": {
    "configure-credentials": "tsx configure-credentials.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.10",
    "dotenv": "^16.4.7",
    "playwright": "^1.49.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
